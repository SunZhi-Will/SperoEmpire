using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.Rendering;

public class SortingGroupLayer : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        //this.GetComponent<SortingGroup>().sortingLayerName = "Default";
        //this.GetComponent<SortingGroup>().sortingOrder = (int)(1f+transform.position.y*-10f);
    }
}
